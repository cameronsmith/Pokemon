const app = {
  /**
   * To make testing easier you can change this to another value like 10.
   * 
   * Please Note: If you'd like to see the loading spinner (as it's very fast locally) you can set this to a value of 0.
   */
  POKEMONS_FETCH_LIMIT: 151,
};

export default app;